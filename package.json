{
    "scripts": {
    "backend": "cd backend && npm start --prefix backend",
    "frontend": "cd ../frontend && npm run dev --prefix frontend",
    "dev": "npm install -g concurrently && concurrently \"npm run backend\" \"npm run frontend\""
    }
}